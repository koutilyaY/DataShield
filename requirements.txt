# Root requirements = the Streamlit demo (streamlit_app.py), pinned to versions
# that ship Python 3.14 wheels (Streamlit Community Cloud builds on 3.14).
# Full FastAPI service deps (Postgres/Kafka/OTel) live in requirements-full.txt.
streamlit==1.58.0
pandas==2.3.3
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.0
