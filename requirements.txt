# Root requirements = the Streamlit demo (streamlit_app.py), so the public
# demo deploys cleanly on Streamlit Community Cloud from `main`.
# For the full FastAPI service (Postgres/Kafka/OTel), use requirements-full.txt.
streamlit==1.41.1
pandas==2.1.3
numpy==1.26.2
scipy==1.11.4
scikit-learn==1.3.2
